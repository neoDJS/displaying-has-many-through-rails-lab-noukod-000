<p id="notice"><%= notice %></p>

<h1>Doctors</h1>

<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to doctor.name, doctor %> <span>, <%= pluralize(doctor.patients.count, 'patient') %> </span></li>
  <% end %>
</ul>

<br>

<%= link_to 'New Doctor', new_doctor_path %>
