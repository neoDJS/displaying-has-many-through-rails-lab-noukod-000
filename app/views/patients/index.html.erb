<p id="notice"><%= notice %></p>

<h1>Patients</h1>

<ul>
  <% @patients.each do |patient| %>
    <li>Name: <%= link_to patient.name, patient %> <br><span> Number of appointments: <%= pluralize(patient.appointments.count, 'appointment') %> </span></li>
  <% end %>
</ul>

<br>

<%= link_to 'New Patient', new_patient_path %>
